<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Typing Delay Tester</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
        
        body {
            background: #1a1a2e;
            color: white;
            padding: 20px;
            line-height: 1.5;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        h1 {
            color: #4cc9f0;
            margin-bottom: 10px;
        }
        
        .stats {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 25px;
        }
        
        .stat-box {
            background: #16213e;
            padding: 15px;
            border-radius: 8px;
            flex: 1;
            min-width: 150px;
            text-align: center;
            border-top: 4px solid #4cc9f0;
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            margin: 5px 0;
        }
        
        .prompt-box {
            background: #0f3460;
            padding: 25px;
            border-radius: 10px;
            margin-bottom: 20px;
            text-align: center;
            font-size: 1.4rem;
            min-height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .input-box {
            margin-bottom: 25px;
        }
        
        #typingInput {
            width: 100%;
            padding: 15px;
            font-size: 1.2rem;
            border: none;
            border-radius: 8px;
            background: #16213e;
            color: white;
            outline: none;
        }
        
        #typingInput:focus {
            box-shadow: 0 0 0 2px #4cc9f0;
        }
        
        .controls {
            display: flex;
            gap: 15px;
            margin-bottom: 30px;
            justify-content: center;
        }
        
        button {
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
        }
        
        #startBtn {
            background: #4cc9f0;
            color: #1a1a2e;
        }
        
        #startBtn:hover {
            background: #3ab0d8;
        }
        
        #resetBtn {
            background: #4361ee;
            color: white;
        }
        
        #resetBtn:hover {
            background: #3a56d4;
        }
        
        .delay-bar {
            height: 10px;
            background: #16213e;
            border-radius: 5px;
            margin: 20px 0;
            overflow: hidden;
        }
        
        #delayIndicator {
            height: 100%;
            width: 0%;
            background: #4cc9f0;
            transition: width 0.3s;
        }
        
        .results {
            background: #16213e;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #0f3460;
        }
        
        th {
            background: #0f3460;
        }
        
        .status-good {
            color: #4ade80;
        }
        
        .status-medium {
            color: #fbbf24;
        }
        
        .status-slow {
            color: #f87171;
        }
        
        .info {
            background: #0f3460;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Typing Delay Tester</h1>
            <p>Measure your typing response time</p >
        </header>
        
        <div class="stats">
            <div class="stat-box">
                <div>Current Delay</div>
                <div class="stat-value" id="currentDelay">0 ms</div>
            </div>
            <div class="stat-box">
                <div>Average Delay</div>
                <div class="stat-value" id="averageDelay">0 ms</div>
            </div>
            <div class="stat-box">
                <div>Tests Done</div>
                <div class="stat-value" id="testsCount">0/5</div>
            </div>
        </div>
        
        <div class="prompt-box" id="promptDisplay">
            Click "Start Test" to begin
        </div>
        
        <div class="input-box">
            <input type="text" id="typingInput" placeholder="Type here when test starts..." disabled>
        </div>
        
        <div class="delay-bar">
            <div id="delayIndicator"></div>
        </div>
        
        <div class="controls">
            <button id="startBtn">Start Test</button>
            <button id="resetBtn">Reset</button>
        </div>
        
        <div class="info">
            <strong>How it works:</strong> Type the text shown above as quickly as possible. 
            Delay is measured from when the text appears to your first keystroke. 
            Complete 5 tests to get your average.
        </div>
        
        <div class="results">
            <h3>Test Results</h3>
            <table id="resultsTable">
                <thead>
                    <tr>
                        <th>Test</th>
                        <th>Delay</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody id="resultsBody">
                    <tr>
                        <td colspan="3" style="text-align: center;">No tests completed yet</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <script>
        // Elements
        const promptDisplay = document.getElementById('promptDisplay');
        const typingInput = document.getElementById('typingInput');
        const startBtn = document.getElementById('startBtn');
        const resetBtn = document.getElementById('resetBtn');
        const currentDelay = document.getElementById('currentDelay');
        const averageDelay = document.getElementById('averageDelay');
        const testsCount = document.getElementById('testsCount');
        const delayIndicator = document.getElementById('delayIndicator');
        const resultsBody = document.getElementById('resultsBody');
        
        // Test data
        let testActive = false;
        let testStartTime = 0;
        let testNumber = 0;
        const maxTests = 5;
        let delays = [];
        
        // Test prompts
        const prompts = [
            "Type this text quickly",
            "Measure your typing speed",
            "Test your reaction time",
            "Keyboard response check",
            "Final typing test prompt"
        ];
        
        // Initialize
        function init() {
            startBtn.addEventListener('click', startTest);
            resetBtn.addEventListener('click', resetTest);
            typingInput.addEventListener('keydown', handleKeydown);
        }
        
        // Start test
        function startTest() {
            if (testActive) return;
            
            resetTest();
            testActive = true;
            testNumber = 1;
            
            typingInput.disabled = false;
            typingInput.focus();
            startBtn.textContent = "Testing...";
            startBtn.disabled = true;
            
            showPrompt();
        }
        
        // Show current prompt
        function showPrompt() {
            if (testNumber > maxTests) {
                endTest();
                return;
            }
            
            promptDisplay.textContent = prompts[testNumber - 1];
            typingInput.value = "";
            testStartTime = Date.now();
            testsCount.textContent = `${testNumber}/${maxTests}`;
            currentDelay.textContent = "0 ms";
            delayIndicator.style.width = "0%";
        }
        
        // Handle typing
        function handleKeydown(e) {
            if (!testActive) return;
            
            // Measure delay on first key press
            if (typingInput.value.length === 0) {
                const delay = Date.now() - testStartTime;
                currentDelay.textContent = `${delay} ms`;
                
                // Update delay bar
                const width = Math.min(delay / 5, 100);
                delayIndicator.style.width = `${width}%`;
                
                // Color based on delay
                if (delay < 150) {
                    delayIndicator.style.backgroundColor = "#4ade80";
                } else if (delay < 300) {
                    delayIndicator.style.backgroundColor = "#fbbf24";
                } else {
                    delayIndicator.style.backgroundColor = "#f87171";
                }
            }
            
            // Submit on Enter
            if (e.key === "Enter") {
                const delay = Date.now() - testStartTime;
                delays.push(delay);
                
                // Add to results table
                addResult(testNumber, delay);
                
                // Move to next test
                testNumber++;
                showPrompt();
            }
        }
        
        // Add result to table
        function addResult(testNum, delay) {
            let status, statusClass;
            
            if (delay < 150) {
                status = "Fast";
                statusClass = "status-good";
            } else if (delay < 300) {
                status = "Medium";
                statusClass = "status-medium";
            } else {
                status = "Slow";
                statusClass = "status-slow";
            }
            
            // Create new row
            const row = document.createElement("tr");
            row.innerHTML = `
                <td>Test ${testNum}</td>
                <td>${delay} ms</td>
                <td class="${statusClass}">${status}</td>
            `;
            
            // If first row placeholder exists, replace it
            if (resultsBody.firstChild.textContent.includes("No tests")) {
                resultsBody.innerHTML = "";
            }
            
            resultsBody.appendChild(row);
            
            // Update average
            updateAverage();
        }
        
        // Update average delay
        function updateAverage() {
            if (delays.length === 0) {
                averageDelay.textContent = "0 ms";
                return;
            }
            
            const sum = delays.reduce((a, b) => a + b, 0);
            const avg = Math.round(sum / delays.length);
            averageDelay.textContent = `${avg} ms`;
        }
        
        // End test
        function endTest() {
            testActive = false;
            typingInput.disabled = true;
            startBtn.disabled = false;
            startBtn.textContent = "Start Test";
            
            promptDisplay.textContent = "Test Complete!";
            
            // Show summary
            if (delays.length > 0) {
                const avg = delays.reduce((a, b) => a + b, 0) / delays.length;
                let message = `Average delay: ${Math.round(avg)}ms. `;
                
                if (avg < 150) message += "Excellent!";
                else if (avg < 250) message += "Good!";
                else if (avg < 350) message += "Average.";
                else message += "Could be better.";
                
                alert("Test Complete!\n" + message);
            }
        }
        
        // Reset test
        function resetTest() {
            testActive = false;
            testNumber = 0;
            delays = [];
            
            promptDisplay.textContent = "Click 'Start Test' to begin";
            typingInput.value = "";
            typingInput.disabled = true;
            
            currentDelay.textContent = "0 ms";
            averageDelay.textContent = "0 ms";
            testsCount.textContent = "0/5";
            delayIndicator.style.width = "0%";
            
            startBtn.disabled = false;
            startBtn.textContent = "Start Test";
            
            // Reset results table
            resultsBody.innerHTML = `
                <tr>
                    <td colspan="3" style="text-align: center;">No tests completed yet</td>
                </tr>
            `;
        }
        
        // Start when page loads
        window.addEventListener('DOMContentLoaded', init);
    </script>
</body>
</html>
